// Supabase CLI entry point for the already deployed function name.
// The application itself remains in ../server so the Figma-generated backend
// and the CLI deployment use one shared implementation.
import "../server/index.tsx";
